name: Ping Backend

on:
  schedule:
    - cron: '*/14 * * * *' 

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
    - name: Send a ping request
      run: |
        curl -X GET https://ping-server-mmip68lso-shreyas-chaudharis-projects-96709d88.vercel.app/api/ping
